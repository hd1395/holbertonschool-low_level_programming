#include <stdio.h>

/**
 * main - prints program name, followed by a new line.
 * @argc: number of arguments
 * @argv: arguments list
 * Return: 0
 */

int main(int argc, char *argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
