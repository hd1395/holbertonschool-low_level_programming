#include "main.h"
/**
 * _isdigit - checks for digit character.
 * @c:Character to check
 *
 * Return: 1 if c is digit (0 -9), 0 otherwise.
 */

int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
