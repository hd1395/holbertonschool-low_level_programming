#include "main.h"
/**
 * main - prints whether the number stored in the variable n
 *is positive or negative.
 * Return: 0
 */
int main(void)
{
print_alphabet();
char c;
for (c = 'a'; c <= 'z'; c++)
putchar(c);
putchar('\n');
return (0);
}
