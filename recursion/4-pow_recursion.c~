#include "main.h"

/**
 * factorial - returns the length of a string.
 * @n: the number to calculate its factorial.
 *
 * Return:  the factorial of a given number, -1 if n < 0.
 */

int factorial(int n)
{
if (n > 0)
return (n * factorial(n - 1));
if (n < 0)
return (-1);
return (1);
}
